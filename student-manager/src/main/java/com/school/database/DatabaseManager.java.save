package database;
import java.sql.Connection;
import java.sql.DriverManager;

public class DatabaseManager {
    public static Connection getConnection() throws Exception {
        // This links Java to the 'school' database you made in MariaDB
        return DriverManager.getConnection("jdbc:mariadb://localhost:3306/school", "root", "");
    }
}

